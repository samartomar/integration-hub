/**
 * Reusable endpoint editing drawer.
 * Used by Auth & Endpoints page and Visual Flow Builder.
 * Same API, layout, and persistence from both contexts.
 */

export { VendorEndpointModal as EndpointEditDrawer } from "./VendorEndpointModal";
