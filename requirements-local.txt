# Local development - Hub API + Lambda layer deps (no AWS SDK required for basic runs)
# Used by: docker-compose hub-api, local uvicorn

# Layer: integrationhub-common
aws-xray-sdk>=2.12.0
jsonschema>=4.20.0
psycopg2-binary>=2.9.0
PyJWT>=2.8.0
# Required for RS256/JWKS JWT signature verification (PyJWT RSA support)
cryptography>=42.0.0
requests>=2.31.0

# Local API server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
